# Frontend Environment Variables

# API Configuration (optional - defaults to /api)
# In production (wms.zeveph.com) the frontend is served by the same Express server,
# so /api resolves correctly without setting this variable.
# VITE_API_URL=/api
